{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "database": "${MONGO_DATABASE}",
    "username": "${MONGO_USERNAME}",
    "password": "${MONGO_PASSWORD}",
    "name": "mongodb",
    "connector": "mongodb"
  }
}
